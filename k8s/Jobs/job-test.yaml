apiVersion: batch/v1
kind: Job
metadata:
  name: pi
spec:
  template:
    spec:
      containers:
        - name: pi
          image: alpine
          command: ["/bin/sh", "-c", "echo The app is running"]
      restartPolicy: OnFailure
  backoffLimit: 4
  ttlSecondsAfterFinished: 0
